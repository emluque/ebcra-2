# TYPE  DATABASE          USER               ADDRESS          METHOD

# postgres superuser — Unix socket only, password required
local   all               postgres                            scram-sha-256

# ebcra application users — specific IPs only
host    estadisticasbcra  estadisticasbcra   172.28.2.2/32    scram-sha-256
host    estadisticasbcra  ebcra-scraping    172.28.2.3/32    scram-sha-256
